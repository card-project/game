package models.cards;

/**
 * Families a card can belong to.  
 * Allow the creation of cards interacting with same family cards
 * and disabling interactions with different family cards. 
 * 
 * @author Simon RENOULT
 * @version 1.0
 */
public enum CardFamily {
	GoStop, Speed, Gas, Tire, StateOfCar;
}
